#!/usr/bin/python3


def no_c(my_string):
    if len(my_string) == 0:
        return None
    new_string = None
    for a in len(my_string):
        if my_string[a] is not 'c' or 'C':
            new_string[a] = my_string[a]
        else:
            new_string[a] = ""

