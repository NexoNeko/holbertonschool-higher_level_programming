#!/usr/bin/python3
def print_last_digit(number):
    if number < 0:
        number = number / -1
    last_digit = abs(number % 10)
    print("{:n}".format(abs(last_digit)), end='')
    return(abs(last_digit))
